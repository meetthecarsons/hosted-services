services:
  bedrock:
    image: itzg/minecraft-bedrock-server
    pull_policy: always
    restart: unless-stopped
    environment:
      EULA: "TRUE"
    ports:
      - "19132:19132/udp"
    volumes:
      - ${DATA_DIR}:/data
    stdin_open: true
    tty: true
